<Window x:Class="ConcentradoKPI.App.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" Width="960" Height="520"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <Border CornerRadius="16" Background="White" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo y título -->
            <StackPanel Grid.Row="0" Margin="0,8,0,16" HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/Assets/SplashScreen.png"
       Height="100" Margin="0,0,0,12"/>
                <TextBlock Text="Concentrado KPI" FontSize="26" FontWeight="Bold" />
                <TextBlock Text="Cargando módulos..." FontSize="14" Foreground="#666"
                           Margin="0,6,0,0" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Barra de progreso -->
            <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="6"
                         VerticalAlignment="Center" Margin="40,0"/>

            <!-- Footer -->
            <TextBlock Grid.Row="2" Text="© 2025 Danny" Opacity="0.6"
                       HorizontalAlignment="Center" Margin="0,16,0,0"/>
        </Grid>
    </Border>
</Window>
